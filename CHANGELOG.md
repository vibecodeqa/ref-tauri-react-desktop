# Changelog

## 0.1.0

- Initial Tauri v2 + Vite + React + TypeScript desktop reference template.
- Narrow note-vault command surface with input validation and Rust unit tests.
- Committed capability files plus a config/capability validator run in CI.
- CI: install, lint, typecheck, Vitest, `cargo fmt`/`clippy`/`test`, and a Linux
  package/build check.
